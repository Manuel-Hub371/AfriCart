{
  "name": "africart",
  "version": "0.1.0",
  "private": true,
  "description": "AfriCart marketplace monorepo. Frontend: Next.js UI (frontend/). Backend: Next.js API (backend/). Root scripts delegate to each workspace.",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "dev:api": "npm --prefix backend run dev",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "start:api": "npm --prefix backend run start",
    "start:ui": "npm --prefix frontend run start",
    "lint": "npm --prefix backend run lint && npm --prefix frontend run lint",
    "db:migrate": "npm --prefix backend run db:migrate",
    "db:migrate:media": "npm --prefix backend run db:migrate:media",
    "db:seed": "npm --prefix backend run db:seed",
    "job:maintenance": "npm --prefix backend run job:maintenance"
  },
  "engines": {
    "node": ">=20"
  }
}