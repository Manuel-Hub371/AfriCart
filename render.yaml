services:
  - type: web
    name: africart
    env: node
    buildCommand: npm install && npx prisma generate && npx prisma db push --accept-data-loss && npm run build
    startCommand: next start -H 0.0.0.0
    healthCheckPath: /
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: africart-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: NEXT_PUBLIC_APP_URL
        value: https://africart-ywef.onrender.com
      - key: NODE_ENV
        value: production

databases:
  - name: africart-db
    plan: starter
    databaseName: africart
